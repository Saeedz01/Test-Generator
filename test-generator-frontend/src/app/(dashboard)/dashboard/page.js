/**
 * Dashboard home placeholder. Compose `features/dashboard` widgets here later.
 */
export default function DashboardPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold text-zinc-900">Dashboard</h1>
      <p className="mt-2 text-zinc-600">
        Overview widgets will live in{" "}
        <code className="text-zinc-800">features/dashboard</code>.
      </p>
    </div>
  );
}
