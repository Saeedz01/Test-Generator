/**
 * =============================================================================
 * styles/
 * =============================================================================
 * Global visual language for the Test Generator education platform.
 *
 * - design-tokens.css → palette, type, space, radius, shadow, motion, control tokens
 * - Import tokens before any component styles so Tailwind utilities resolve.
 *
 * No pages or components live here — tokens only.
 * =============================================================================
 */
