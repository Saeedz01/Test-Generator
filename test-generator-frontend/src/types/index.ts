/**
 * =============================================================================
 * types
 * =============================================================================
 * Shared domain and API shape definitions (TypeScript).
 *
 * Prefer importing named types from `@/types` or `@/types/entities`.
 * =============================================================================
 */

export type {
  SchoolClass,
  Book,
  Chapter,
  QuestionType,
  QuestionDifficulty,
  QuestionBase,
  McqQuestion,
  ShortQuestion,
  LongQuestion,
  Question,
  ChapterWithQuestions,
  BookWithChapters,
  ClassWithBooks,
} from "./entities";
