/**
 * =============================================================================
 * components
 * =============================================================================
 * Cross-cutting React UI shared by multiple features and routes.
 *
 * SUBFOLDERS
 * - ui/       → design-system primitives
 * - layouts/  → shared page chrome (header, sidebar, shells)
 *
 * Redux / app providers live in `store/providers.tsx`, not here.
 * Prefer importing from a subfolder (e.g. `@/components/ui`).
 * =============================================================================
 */

export {};
