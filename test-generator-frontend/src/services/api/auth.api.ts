/**
 * =============================================================================
 * services/api/auth.api.ts
 * =============================================================================
 * Auth endpoints (login, register, OTP, password reset).
 * Aligns with NestJS `modules/auth`.
 *
 * Inject into `baseApi` when implementing; export generated hooks for features.
 * =============================================================================
 */

export {};
