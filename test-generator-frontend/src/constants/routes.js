/**
 * Central route path constants.
 * Keeps navigation links and redirects consistent across the app.
 * No logic — string values only.
 */
export const ROUTES = {
  HOME: "/",
  LOGIN: "/login",
  REGISTER: "/register",
  DASHBOARD: "/dashboard",
  QUESTIONS: "/dashboard/questions",
  BOOKS: "/dashboard/books",
  CHAPTERS: "/dashboard/chapters",
  CLASSES: "/dashboard/classes",
};
