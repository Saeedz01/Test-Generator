/**
 * =============================================================================
 * assets/
 * =============================================================================
 * Static media and design assets imported by the UI.
 *
 * WHAT BELONGS HERE
 * - Images, icons (non-lucide), illustrations, fonts (if self-hosted), Lottie
 * - Organized by kind when the set grows (e.g. assets/images, assets/icons)
 *
 * WHAT DOES NOT BELONG HERE
 * - Files that must be publicly URL-addressable without a bundler import
 *   (those stay in `/public`)
 * - CSS / Tailwind tokens (→ app/globals.css or theme config)
 *
 * Prefer importing from here so Next.js can optimize images and hash filenames.
 * =============================================================================
 */

export {};
