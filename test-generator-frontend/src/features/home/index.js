/**
 * =============================================================================
 * features/home
 * =============================================================================
 * Public marketing homepage sections (hero, classes, features, stats, CTA).
 * Composes shared UI primitives + dummy data only — no API logic.
 * =============================================================================
 */

export {
  HomeHero,
  FeaturedClassesSection,
  FeaturesSection,
  StatisticsSection,
  HomeCtaSection,
} from "./components";
