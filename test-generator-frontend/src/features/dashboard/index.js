/**
 * =============================================================================
 * features/dashboard
 * =============================================================================
 * Authenticated home / overview surface (widgets, summaries, shortcuts).
 * Composes other features; keep orchestration thin until data is wired.
 * =============================================================================
 */

export {};
