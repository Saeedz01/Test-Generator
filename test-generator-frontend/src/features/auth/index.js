/**
 * =============================================================================
 * features/auth
 * =============================================================================
 * Authentication & session domain (login, register, OTP, password reset).
 * Aligns with NestJS `modules/auth`.
 *
 * - components/ → auth UI
 * - hooks/      → auth-specific hooks (consume services/api/auth.api later)
 * - utils/      → auth-only helpers (validation mappers, etc.)
 *
 * API endpoints: `services/api/auth.api.ts` (RTK Query).
 * =============================================================================
 */

export {};
